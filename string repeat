#include <stdio.h>
#include<string.h>
int main()
{
    char str[50];
    scanf("%s",str);
    int i,n,j;
    n =strlen(str);
    for(i=1;i<n;i=i+2)
    {
    int r=(int)str[i]-48;
    for(j=1;j<=r;j++){
        printf("%c",str[i-1]);
    }
        
        
    }
    
    return 0;
}
