#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    char str1[50],   str2[50];
    scanf("%s",str1);
    scanf("%s",str2);
    int n=strcmp(str1,str2);
    if(n==0){
      printf("Strings are same");
    }
    else
        printf("Strings are not same");
    return 0;
}
